data/careers_*.jsonl
data/*.state.json
data/*.log
data/institutions.json
data/traffic.jsonl
data/*.bak
.env
scripts/__pycache__/

# daily.sh's lock file (runtime state; the pid was committed once by mistake, and releasing the lock then showed up
# as an uncommitted deletion for ever after)
data/.daily.lock/
__pycache__/
