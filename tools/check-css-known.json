{
  "_what": "Items check-css.mjs reports that have been looked at and ruled on: the key is the name it reports, the value is the reason.",
  "_how": "A reason must state only the step that was actually verified, never more than the check behind it can support. When an entry is no longer reported, this check reports it as STALE and fails.",
  "dead": {
    "R1": "Set by `class=\"tier ${it.tier}\"` at `index.html:817` and `class=\"tier ${v.tier.replace('V','R')}\"` at `index.html:966`; `it.tier` takes the values R1–R4 (the four rules `.tier.R1{--tier-fill:100%}` and the rest are on line 121). The name is built at run time, so a static check cannot see it being set.",
    "R2": "Set by `class=\"tier ${it.tier}\"` at `index.html:817` and `class=\"tier ${v.tier.replace('V','R')}\"` at `index.html:966`; `it.tier` takes the values R1–R4 (the four rules `.tier.R1{--tier-fill:100%}` and the rest are on line 121). The name is built at run time, so a static check cannot see it being set.",
    "R3": "Set by `class=\"tier ${it.tier}\"` at `index.html:817` and `class=\"tier ${v.tier.replace('V','R')}\"` at `index.html:966`; `it.tier` takes the values R1–R4 (the four rules `.tier.R1{--tier-fill:100%}` and the rest are on line 121). The name is built at run time, so a static check cannot see it being set.",
    "R4": "Set by `class=\"tier ${it.tier}\"` at `index.html:817` and `class=\"tier ${v.tier.replace('V','R')}\"` at `index.html:966`; `it.tier` takes the values R1–R4 (the four rules `.tier.R1{--tier-fill:100%}` and the rest are on line 121). The name is built at run time, so a static check cannot see it being set.",
    "on": "Set by `class=\"${k===S.stratum?'on':''}\"` at `index.html:679` (the selected state of the stratum segmented control); its CSS is `.seg button.on{background:var(--paper);…}`. The template builds the name with a ternary, so a static check sees only the `${k===S.stratum?` part, which is the first `built` entry of this file.",
    "btn-ghost": "Checked 2026-09-22: across the repository, in class attributes, class: properties, classList and className assignments, the name does not appear **literally**. That supports \"nothing writes it\"; it **does not prove that nothing sets it**, because a name built at run time cannot be seen this way, and the four `built` entries of this file are where this repository builds names. Kept rather than deleted: removing rules that come with the shared style package is a separate decision.",
    "btn-icon": "Checked 2026-09-22: across the repository, in class attributes, class: properties, classList and className assignments, the name does not appear **literally**. That supports \"nothing writes it\"; it **does not prove that nothing sets it**, because a name built at run time cannot be seen this way, and the four `built` entries of this file are where this repository builds names. Kept rather than deleted: removing rules that come with the shared style package is a separate decision.",
    "data": "Checked 2026-09-22: across the repository, in class attributes, class: properties, classList and className assignments, the name does not appear **literally**. That supports \"nothing writes it\"; it **does not prove that nothing sets it**, because a name built at run time cannot be seen this way, and the four `built` entries of this file are where this repository builds names. Kept rather than deleted: removing rules that come with the shared style package is a separate decision.",
    "nav": "Checked 2026-09-22: across the repository, in class attributes, class: properties, classList and className assignments, the name does not appear **literally**. That supports \"nothing writes it\"; it **does not prove that nothing sets it**, because a name built at run time cannot be seen this way, and the four `built` entries of this file are where this repository builds names. Kept rather than deleted: removing rules that come with the shared style package is a separate decision.",
    "pill": "Checked 2026-09-22: across the repository, in class attributes, class: properties, classList and className assignments, the name does not appear **literally**. That supports \"nothing writes it\"; it **does not prove that nothing sets it**, because a name built at run time cannot be seen this way, and the four `built` entries of this file are where this repository builds names. Kept rather than deleted: removing rules that come with the shared style package is a separate decision.",
    "plate": "Checked 2026-09-22: across the repository, in class attributes, class: properties, classList and className assignments, the name does not appear **literally**. That supports \"nothing writes it\"; it **does not prove that nothing sets it**, because a name built at run time cannot be seen this way, and the four `built` entries of this file are where this repository builds names. Kept rather than deleted: removing rules that come with the shared style package is a separate decision.",
    "sr-only": "Checked 2026-09-22: across the repository, in class attributes, class: properties, classList and className assignments, the name does not appear **literally**. That supports \"nothing writes it\"; it **does not prove that nothing sets it**, because a name built at run time cannot be seen this way, and the four `built` entries of this file are where this repository builds names. Kept rather than deleted: removing rules that come with the shared style package is a separate decision."
  },
  "built": {
    "${k===S.stratum?": "The selected state of the stratum segmented control; it produces one name, `on` (see dead.on).",
    "${it.tier}": "The tier badge of the institutions table; it produces R1–R4 (see dead.R1–R4).",
    "${v.tier.replace(": "The tier badge of the journals table: it turns V1–V4 into R1–R4, the same set of names as the entry above.",
    "${cls}": "The sortable table headers at `index.html:939`; `cls` is passed in by four call sites (lines 1035/1036 and 964). The literal values found are `''`, `'num'`, `'num hide-s'`, and the column taken from the journals header table on line 964. Each of these names also appears literally elsewhere in index.html, so none has a `dead` entry. Every entry of the table on line 964 was checked: its values also fall within `num`, `hide-s` and empty."
  },
  "unstyled": {}
}
